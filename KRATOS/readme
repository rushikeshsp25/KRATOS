Important to run system in proper state when run first time
1] craete group permissions objects
    a] view
    b] view_add
    c] view_add_edit
    d] view_add_edit_delete
    e] superuser
    login using admin go to group and create above groups

2] to add debit atleast 1 user,1 system and 1 category needs to be crated
